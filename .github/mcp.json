{
  "mcpServers": {
    "mlflow": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-lc",
        "set -a; [ -f .env ] && source .env; set +a; export MLFLOW_TRACKING_URI=\"$MLFLOW_BACKEND_STORE\"; exec uv run --with 'mlflow[mcp]>=3.7.0' mlflow mcp run"
      ]
    }
  }
}
