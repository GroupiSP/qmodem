We recommend using `uv` for contributing.

## Install pre-commit hooks
Before starting contributing, make sure to install the pre-commit hooks,

```bash
uv run pre-commit install
```

## Run tests
Run `pytest` via `uv`,

```bash
uv run pytest
```
